<%@ page contentType="text/html; charset=UTF-8" language="java" %>
<%
    com.tms.beans.UserBean userBean = 
        (com.tms.beans.UserBean) request.getAttribute("userBean");
    String section = (String) request.getAttribute("section");
    String error   = (String) request.getAttribute("error");
%>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>        <!-- JSTL 3.0 core -->
<%@ taglib prefix="fmt" uri="jakarta.tags.fmt" %>         <!-- JSTL 3.0 fmt -->
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>User Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body>
  <!-- your existing navbar -->
  <jsp:include page="UserHome.jsp"/>

  <div class="container my-4">
    <!-- Nav pills -->
    <ul class="nav nav-pills mb-3">
      <li class="nav-item">
        <a class="nav-link ${section=='home'? 'active': ''}" href="userprofile">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link ${section=='view'? 'active': ''}" href="viewuserprofile">View Profile</a>
      </li>
      <li class="nav-item">
        <a class="nav-link ${section=='edit'? 'active': ''}" href="edituserprofile">Edit Profile</a>
      </li>
      <li class="nav-item ms-auto">
        <button class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#changePwdModal">
          Change Password
        </button>
      </li>
    </ul>

    <div class="tab-content">
      <!-- Home -->
      <div class="tab-pane fade ${section=='home'? 'show active': ''}">
        <div class="alert alert-info">
          Hey <strong>${userBean.fName}</strong>! Welcome to NITRTC. Choose an option above.
        </div>
      </div>

      <!-- View Profile -->
      <div class="tab-pane fade ${section=='view'? 'show active': ''}">
        <h4>Profile Details</h4>
        <table class="table table-bordered">
          <tr><th>First Name</th><td>${userBean.fName}</td></tr>
          <tr><th>Last Name</th><td>${userBean.lName}</td></tr>
          <tr><th>Email</th><td>${userBean.mailId}</td></tr>
          <tr><th>Address</th><td>${userBean.addr}</td></tr>
          <tr><th>Phone</th><td>${userBean.phNo}</td></tr>
        </table>
      </div>

      <!-- Edit Profile -->
      <div class="tab-pane fade ${section=='edit'? 'show active': ''}">
        <h4>Edit Your Profile</h4>
        <form action="updateuserprofile" method="post" class="row g-3">
          <div class="col-md-6">
            <label class="form-label">First Name</label>
            <input type="text" name="firstname" class="form-control"
                   value="${userBean.fName}" required/>
          </div>
          <div class="col-md-6">
            <label class="form-label">Last Name</label>
            <input type="text" name="lastname" class="form-control"
                   value="${userBean.lName}" required/>
          </div>
          <div class="col-12">
            <label class="form-label">Address</label>
            <input type="text" name="address" class="form-control"
                   value="${userBean.addr}" required/>
          </div>
          <div class="col-md-6">
            <label class="form-label">Phone</label>
            <input type="text" name="phone" class="form-control"
                   value="${userBean.phNo}" required/>
          </div>
          <input type="hidden" name="mail" value="${userBean.mailId}"/>
          <div class="col-12">
            <button type="submit" class="btn btn-primary">Update Profile</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Change Password Modal -->
  <div class="modal fade" id="changePwdModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
      <form action="changeuserpwd" method="post" class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Change Password</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <c:if test="${not empty error}">
            <div class="alert alert-danger">${error}</div>
          </c:if>
          <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="email" name="username" class="form-control"
                   value="${userBean.mailId}" readonly/>
          </div>
          <div class="mb-3">
            <label class="form-label">Old Password</label>
            <input type="password" name="oldpassword" class="form-control" required/>
          </div>
          <div class="mb-3">
            <label class="form-label">New Password</label>
            <input type="password" name="newpassword" class="form-control" required/>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary"
                  data-bs-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-success">Change Password</button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // If we came back with an error, auto-open the modal
    <c:if test="${section=='change' && not empty error}">
      new bootstrap.Modal(document.querySelector('#changePwdModal')).show();
    </c:if>
  </script>
</body>
</html>
